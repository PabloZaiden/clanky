/**
 * Compatibility exports for callers that still use the historical Core path.
 */

export {
  DomainError,
  isDomainError,
} from "../domain/domain-error";
export type {
  DomainErrorOptions,
} from "../domain/domain-error";
