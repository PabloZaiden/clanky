name: Build and Release Binaries

on:
  release:
    types:
      - published

jobs:
  binaries:
    uses: pablozaiden/installer/.github/workflows/reusable-binary-release.yml@d65a93c1af0d7ce62524d6ccdfee44d0f6ceac4e # v0.0.7
    permissions:
      contents: write
    with:
      install_command: bun install --frozen-lockfile
      prebuild_command: bun run build
      binaries: |
        [
          {
            "name": "clanky",
            "asset_prefix": "clanky",
            "build_command": "bun src/build.ts --target=$BUN_TARGET",
            "output_path": "dist/clanky-$RELEASE_TARGET"
          }
        ]
